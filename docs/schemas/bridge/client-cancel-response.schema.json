{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mfow/ocaml-temporal/docs/schemas/bridge/client-cancel-response.schema.json",
  "title": "OCaml Temporal native client cancellation acknowledgement",
  "description": "The Rust bridge emits this closed document only after Temporal accepts the cancellation RPC. It is an acknowledgement, not proof that the workflow has reached its cancelled terminal state; callers must use the exact-run wait operation.",
  "type": "object",
  "additionalProperties": false,
  "required": ["acknowledged"],
  "properties": {
    "acknowledged": {"const": true}
  }
}
