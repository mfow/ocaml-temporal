{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/mfow/ocaml-temporal/docs/schemas/bridge/client-start-ticket.schema.json",
  "title": "OCaml Temporal native client asynchronous start ticket",
  "description": "Opaque ticket returned by the private begin-start operation and supplied unchanged to poll or bounded-wait operations.",
  "type": "object",
  "additionalProperties": false,
  "required": ["ticket"],
  "properties": {
    "ticket": {"type": "string", "minLength": 1}
  }
}
